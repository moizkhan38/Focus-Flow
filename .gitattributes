# Shell scripts and Dockerfiles MUST stay LF — CRLF breaks the shebang and
# COPY'd scripts inside Linux containers (`/bin/sh^M: bad interpreter`).
*.sh            text eol=lf
docker-entrypoint.sh text eol=lf
Dockerfile      text eol=lf
*.dockerignore  text eol=lf
nginx.conf      text eol=lf
